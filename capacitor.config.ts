import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zpower.careersim',
  appName: 'My Football Career Sim',
  webDir: 'dist'
};

export default config;
